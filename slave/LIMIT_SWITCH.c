#include "LIMIT_SWITCH.h"
#include "DIO.h"

#define LIMIT_PORT PORTC_ID
#define LIMIT_PIN  PIN3

void LIMIT_SWITCH_Init(void)
{
	DIO_SetPinDirection(LIMIT_PORT, LIMIT_PIN, PIN_INPUT);

	/* ????? ??? Pull-up ??????? (??????? ????? ??????) */
	DIO_SetPinValue(LIMIT_PORT, LIMIT_PIN, PIN_HIGH);
}

u8 LIMIT_SWITCH_IsPressed(void)
{
	/* ??????? ????? ??? ????? ???? LOW (???? ????? ?? GND) */
	return (DIO_GetPinValue(LIMIT_PORT, LIMIT_PIN) == PIN_LOW) ? 1 : 0;
}